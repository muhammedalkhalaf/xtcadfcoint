% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/methods.R
\name{is_cointegrated}
\alias{is_cointegrated}
\title{Check Cointegration Rejection}
\usage{
is_cointegrated(object, level = 0.05)
}
\arguments{
\item{object}{An object of class \code{"xtcadfcoint"}.}

\item{level}{Significance level (e.g., 0.05 for 5\%). Default is 0.05.}
}
\value{
Logical indicating whether H0 is rejected (TRUE = cointegrated).
}
\description{
Determines whether the null of no cointegration is rejected at a
given significance level.
}
\details{
Requires that bootstrap critical values were simulated (simulate > 0).
The test rejects the null of no cointegration if the CIPS statistic
is below the critical value (left-tail test).
}
\examples{
\donttest{
# Example with simulated CVs
set.seed(123)
N <- 10; TT <- 50
panel_data <- data.frame(
  id = rep(1:N, each = TT),
  time = rep(1:TT, N),
  y = rnorm(N * TT),
  x = rnorm(N * TT)
)
result <- xtcadfcoint(y ~ x, data = panel_data,
                     id = "id", time = "time",
                     simulate = 100)
is_cointegrated(result, level = 0.05)
}

}
